@model Library.Models.Models.TravelPackageCityAttraction

@{
    ViewData["Title"] = "Edit";
    var tpc = (Library.Models.Models.TravelPackageCity)ViewBag.TravelPackageCity;
}

<h1>Edit</h1>

<h4>TravelPackageCityAttraction</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="hidden" asp-for="TravelPackageCityId" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="CityAttractionId" class="control-label"></label>
                <select asp-for="CityAttractionId" class="form-control" asp-items="ViewBag.CityAttractions">
                    <option value="">--Select Attraction</option>
                </select>
                <span asp-validation-for="CityAttractionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    @Html.ActionLink("Back", "Index", new { id = Model.TravelPackageCityId })
</div>

