@model Domain.Data.DataTransferObjects.DTOUserRole

@{
    ViewData["Title"] = "ConfigureNewStaff";

}

<h1>Configure New Staff</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ConfigureNewStaff">
            <input hidden asp-for="RoleId" class="form-control" />
            <div class="form-group">
                <label asp-for="UserId" class="control-label">Course</label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.staffList">
                    <option value="">-- select user --</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="ConfigureNewStaff" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

